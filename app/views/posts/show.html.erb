<div class="detail-main">
  <div class="article-wrapper">
    <div class="article-haeder">
      <p>
      <%= link_to "名前", "#" %>さんが
      <%= @article.created_at.strftime '%Y年%-m月%-d日' %>に投稿
      </p>
      <h2><%= @article.title %></h2>
    </div>
    <div class="article-main">
      <%= @article.article1 %><br />
      <% if @article.image1.present? %>
      <%= image_tag @article.image1 %>
      <% end %>
    </div>
  </div>
  <div class="article-operation">
    <% if current_user.id == @article.user_id %>
          <%= link_to "商品の編集", edit_post_path(@article.id), method: :get, class: "item-red-btn" %>
          <p class="or-text">or</p>
          <%= link_to "削除", post_path(@article.id), method: :delete, class:"item-destroy" %>
    <% end %>
  <div>
</div>